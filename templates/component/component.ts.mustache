import { FC } from "react";

import { {{component}}Props } from "./{{component}}.model";

import s from "./{{component}}.module.scss";

export const {{component}}: FC<{{component}}Props> = (props: {{component}}Props) => {
    return <div>{{component}} works!</div>;`
}